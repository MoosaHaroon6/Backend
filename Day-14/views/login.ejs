<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login</title>
</head>

<body class="bg-zinc-900 text-zinc-100">
    <main class="min-h-screen flex items-center justify-center p-4">
        <div class="bg-zinc-800 p-8 rounded-xl shadow-xl max-w-md w-full space-y-6">
            <div class="space-y-2 text-center">
                <h1 class="text-3xl font-bold">Sign in to your account</h1>
                <p class="text-zinc-400">Enter your credentials to continue</p>
            </div>

            <form class="space-y-4" action="/login" method="post">
                <div>
                    <label for="email" class="block text-sm font-medium mb-1">Email</label>
                    <input type="email" id="email"
                        class="w-full px-4 py-2 bg-zinc-700 border border-zinc-600 rounded-lg focus:ring-2 focus:ring-zinc-500 focus:border-transparent outline-none transition"
                        placeholder="name@example.com" name="email" required>
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium mb-1">Password</label>
                    <input type="password" id="password"
                        class="w-full px-4 py-2 bg-zinc-700 border border-zinc-600 rounded-lg focus:ring-2 focus:ring-zinc-500 focus:border-transparent outline-none transition"
                        placeholder="••••••••" name="password" required>
                </div>

                <div class="flex items-center justify-between">
                    <label class="flex items-center space-x-2">
                        <input type="checkbox"
                            class="rounded bg-zinc-700 border-zinc-600 text-zinc-500 focus:ring-zinc-500">
                        <span class="text-sm">Remember me</span>
                    </label>
                    <a href="#" class="text-sm text-zinc-400 hover:text-zinc-300 transition">Forgot password?</a>
                </div>

                <button type="submit"
                    class="w-full py-2 px-4 bg-zinc-600 hover:bg-zinc-500 rounded-lg font-medium transition-colors">
                    Sign in
                </button>
            </form>

            <p class="text-center text-zinc-400">
                Don't have an account?
                <a href="/" class="text-zinc-300 hover:text-white transition-colors">Create Account</a>
            </p>
        </div>
    </main>
</body>

</html>