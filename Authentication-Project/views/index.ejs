<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Create User</title>
</head>

<body class="w-full h-full bg-zinc-800">
    <div class="w-full h-full p-10 flex justify-center items-center">
        <form action="/create" method="post" class="w-full max-w-md bg-zinc-700 p-8 rounded-lg shadow-lg">
            <h1 class="text-2xl font-bold text-white mb-8 text-center">Create Account</h1>

            <div class="mb-6">
                <input type="text" name="name"
                    class="w-full p-3 bg-zinc-600 text-white placeholder-zinc-400 rounded-md outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all"
                    placeholder="Username" />
            </div>

            <div class="mb-6">
                <input type="email" name="email"
                    class="w-full p-3 bg-zinc-600 text-white placeholder-zinc-400 rounded-md outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all"
                    placeholder="Email" />
            </div>

            <div class="mb-8">
                <input type="password" name="password"
                    class="w-full p-3 bg-zinc-600 text-white placeholder-zinc-400 rounded-md outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all"
                    placeholder="Password" />
            </div>

            <div class="mb-8">
                <input type="text" name="image"
                    class="w-full p-3 bg-zinc-600 text-white placeholder-zinc-400 rounded-md outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all"
                    placeholder="Image URL" />
            </div>

            <button type="submit"
                class="w-full p-3 bg-purple-600 text-white font-semibold rounded-md hover:bg-purple-700 transition-colors">
                Create Account
            </button>

            <a href="/read" class="text-green-600 hover:underline hover:text-green-8    00">See All Users</a>
        </form>
    </div>
</body>

</html>