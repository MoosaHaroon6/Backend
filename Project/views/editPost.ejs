<!DOCTYPE html>
<html lang="en" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <title>Edit</title>
</head>

<body class="bg-zinc-900 text-white h-full flex flex-col">
    <main class="flex-1">
        <div class="max-w-2xl mx-auto h-full p-4 flex flex-col gap-6">
            <div>
                <p class="text-zinc-300 mb-4">Edit Post</p>
                <form action="/edit/<%= post._id %>" method="post" class="flex flex-col gap-4">
                    <textarea name="content" class="w-full p-3 bg-zinc-700 text-white rounded-lg resize-none 
                               focus:outline-none focus:ring-2 focus:ring-blue-500" rows="8"
                        placeholder="Write something..."><%= console.log(post.content) %></textarea>
                    <button type="submit" class="w-full bg-purple-600 text-white py-2 px-4 rounded-lg 
                               hover:bg-purple-700 focus:outline-none focus:ring-2 
                               focus:ring-purple-500 transition duration-200">
                        Save Post
                    </button>
                </form>
            </div>
        </div>
    </main>
</body>

</html>
