<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Notepad</title>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">

                <input type="text" placeholder="Blog Title" name="title" required
                    class="block px-6 py-4 bg-zinc-800 mt-4 w-full rounded-md text-gray-300">

                <textarea placeholder="Blog Description" required
                    class="block px-6 mt-2 w-full py-5 text-gray-300 bg-zinc-800 resize-none"
                    name="description"></textarea>

                <button class="bg-blue-600 text-white w-24 h-10 rounded-md mt-4">Create File</button>
            </form>
        </div>
        <div class="tasks p-10 flex flex-wrap gap-5">
            <% if (files.length> 0) { %>
                <% files.forEach((file)=> { %>
                    <div class="min-w-72 px-4 py-4 bg-zinc-800 rounded-md">
                        <h2 class="text-2xl tracking-tighter text-gray-300">
                            <%= file %>
                        </h2>
                        <div class="flex mt-4 justify-between gap-5 items-center">
                            <a href="/testFiles/<%= file %>" class="text-blue-600 text-sm">Show Description...</a>
                            <div class="flex w-24 justify-between">
                                <!-- Edit Button -->

                                <a class="text-green-600 rounded-lg cursor-pointer text-lg hover:text-green-800"
                                    href="/edit/<%= file %>">Edit</a>

                                <!-- Edit Button -->

                                <!-- Delete Button -->

                                <a class="text-red-600 rounded-lg cursor-pointer text-lg">
                                    <form action="/delete" method="post"
                                        onsubmit="return confirm('Are you sure you want to delete this file?');"
                                        style="display: inline;">
                                        <input type="hidden" name="filename" value="<%= file %>">
                                        <button type="submit" class="text-red-600  hover:text-red-800">Delete</button>
                                    </form>
                                </a>

                                <!-- Delete Button -->

                            </div>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <h2 class="text-gray-400">No Tasks Available.</h2>
                            <% } %>
        </div>
    </div>
</body>

</html>