<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>All Users</title>
</head>

<body class="w-full h-full bg-zinc-800">
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <h1 class="text-white text-4xl font-bold mb-8">All Users</h1>
        <div class="users-div grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            <% if(users.length> 0) { %>
                <% users.forEach((user)=> { %>
                    <div
                        class="user p-6 bg-zinc-800 rounded-lg shadow-lg hover:shadow-xl transition-shadow duration-300">
                        <div class="w-full h-48 rounded-lg bg-zinc-700 mb-6 overflow-hidden">
                            <img src="<%= user.image %>" class="w-full h-full object-fit" alt="<%= user.name %>">
                        </div>
                        <h3 class="text-xl font-semibold tracking-tight mb-2">
                            <%= user.name %>
                        </h3>
                        <p class="text-zinc-400 mb-4">
                            <%= user.email %>
                        </p>
                        <div class="flex gap-4">
                            <a href="/edit"
                                class="text-blue-500 hover:text-blue-600 underline transition-colors duration-200">Edit</a>
                            <a href="/delete/<%= user._id %>"
                                class="text-red-500 hover:text-red-600 underline transition-colors duration-200">Delete</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <p class="text-2xl text-zinc-700">No users found.</p>
                            <% } %>
        </div>
    </div>
</body>

</html>